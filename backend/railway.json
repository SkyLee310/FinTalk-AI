{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "//": "No npm ci here. Nixpacks installs dependencies in an earlier layer and mounts a build cache at node_modules/.cache; a second npm ci tries to remove node_modules and fails with EBUSY on that mount.",
    "buildCommand": "npm run db:generate && npm run build"
  },
  "deploy": {
    "startCommand": "npm run db:deploy && npm run db:constraints && npm start",
    "healthcheckPath": "/health",
    "restartPolicyType": "ON_FAILURE"
  }
}
