{
  "name": "fintalk-backend",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "tsx watch --env-file=.env src/server.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "typecheck": "tsc --noEmit && tsc -p tsconfig.test.json",
    "lint": "eslint .",
    "test": "vitest run",
    "test:unit": "vitest run tests/unit",
    "test:integration": "vitest run tests/integration",
    "db:generate": "prisma generate",
    "db:migrate": "prisma migrate dev",
    "db:deploy": "node scripts/fix-failed-migrations.mjs && prisma migrate deploy",
    "db:constraints": "node scripts/apply-constraints.mjs",
    "db:reset": "prisma migrate reset --force && npm run db:constraints",
    "db:seed": "tsx --env-file=.env prisma/seed.ts",
    "db:seed:deploy": "tsx prisma/seed.ts",
    "backfill:embeddings": "node dist/scripts/backfill-embeddings.js"
  },
  "prisma": {
    "seed": "tsx prisma/seed.ts"
  },
  "dependencies": {
    "@fastify/cookie": "^11.1.2",
    "@fastify/cors": "^10.0.1",
    "@fastify/multipart": "^10.1.0",
    "@google/genai": "^2.16.0",
    "@prisma/client": "^6.2.0",
    "argon2": "^0.41.1",
    "dotenv": "^16.4.7",
    "fastify": "^5.2.0",
    "jose": "^5.10.0",
    "mammoth": "^1.12.1",
    "pg": "^8.13.1",
    "prisma": "^6.2.0",
    "tsx": "^4.19.2",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@eslint/js": "^9.17.0",
    "@types/jszip": "^3.4.0",
    "@types/node": "^22.10.0",
    "@types/pg": "^8.11.10",
    "eslint": "^9.17.0",
    "jszip": "^3.10.1",
    "typescript": "^5.7.2",
    "typescript-eslint": "^8.19.0",
    "vitest": "^2.1.8"
  }
}
